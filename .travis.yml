sudo: required

language: R

services:
  - docker

before_install:
  - travis_wait 30 docker build -t diegovalle/elcrimen-docker .

deploy:
  provider: script
  script: bash docker_push
  on:
    branch: master

branches:
  only:
    - master